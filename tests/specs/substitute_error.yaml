---
test:
  some: ${{values.some.value}}
  suite: ${{env.TEST_SUITE}}

---
values:
  some:
    value: Awesome!
result:
  err:
    msg: "missing variable: env.TEST_SUITE"

---
values: {}
env:
  suite: unit
result:
  err:
    msg: "missing variable: values.some"
